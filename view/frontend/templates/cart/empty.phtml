<?php
/**  @var $block \Magento\Checkout\Block\Cart */
?>
<?= $block->getChildHtml('checkout_cart_empty_widget') ?>
<div class="max-w-md mx-auto my-12 p-8 bg-white border border-gray-200 shadow-md rounded-lg text-center space-y-6">
    <div class="mx-auto w-20 h-20 bg-blue-50 text-blue-600 rounded-full flex items-center justify-center">
        <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path>
        </svg>
    </div>
    <div class="space-y-2">
        <h1 class="text-2xl font-bold text-gray-900"><?= $block->escapeHtml(__('Empty Cart')) ?></h1>
        <p class="text-sm text-gray-500">
            <?= $block->escapeHtml(__('Looks like there\'s nothing here yet. Add something to your shopping cart to get started.')) ?>
        </p>
    </div>
    <div class="pt-4 flex flex-col gap-3">
        <a class="btn btn-primary w-full"
           href="<?= $block->escapeUrl($block->getUrl()) ?>"
           title="<?= $block->escapeHtmlAttr(__('Continue Shopping')) ?>">
            <?= $block->escapeHtml(__('Continue Shopping')) ?>
        </a>
        <?php if ($block->getQuote()->getCustomer()->getId()): ?>
            <a class="btn btn-secondary w-full"
               href="<?= $block->escapeUrl($block->getUrl('customer/account')) ?>"
               title="<?= $block->escapeHtmlAttr(__('My Account')) ?>">
                <?= $block->escapeHtml(__('My Account')) ?>
            </a>
        <?php else: ?>
            <a class="btn btn-secondary w-full"
               href="<?= $block->escapeUrl($block->getUrl('customer/account/login')) ?>"
               title="<?= $block->escapeHtmlAttr(__('Log In')) ?>">
                <?= $block->escapeHtml(__('Log In')) ?>
            </a>
        <?php endif; ?>
    </div>
</div>
<?= $block->getChildHtml('shopping.cart.table.after') ?>
